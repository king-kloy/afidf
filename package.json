{
  "name": "afidf",
  "version": "1.0.0",
  "description": "project for African International Documentary Festival",
  "scripts": {
    "start": "cd backend && node server.js",
    "heroku-postbuild": "cd frontend && npm install --only=dev && npm install && npm run build",
    "predeploy": "cd frontend && npm run build",
    "deploy": "cd frontend && gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/king-kloy/afidf.git"
  },
  "author": "Okai Yeboah",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/king-kloy/afidf/issues"
  },
  "homepage": "https://king-kloy.github.io/afidf",
  "dependencies": {
    "concurrently": "^5.0.0",
    "gh-pages": "^2.1.1"
  }
}
